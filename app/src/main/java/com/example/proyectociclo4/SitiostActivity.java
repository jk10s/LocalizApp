package com.example.proyectociclo4;

import androidx.appcompat.app.AppCompatActivity;
import androidx.recyclerview.widget.LinearLayoutManager;
import androidx.recyclerview.widget.RecyclerView;

import android.os.Bundle;
import android.widget.ArrayAdapter;
import android.widget.ListView;

import com.example.proyectociclo4.modelos.Sitio;

import java.util.ArrayList;

public class SitiostActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_sitiost);

       //RecyclerView rcvSitios = (RecyclerView) findViewById(R.id.versitiosrecy);
       // rcvSitios.setLayoutManager(new LinearLayoutManager(this));
        //SitioDOA  sitioADO db = new sitioADO
        //ArrayList<Sitio> sitios = new ArrayList<>();

    }
}